任务来源: Feishu Webhook
任务内容: Still smtp error, tell me what info do you need to solve the problem

代码修改摘要:
1. 更新 `tools/send_email.py`：
   - 新增 `build_smtp_info_request(...)`，在 SMTP 发送失败时生成结构化排障信息清单。
   - 失败提示中明确说明需要提供的信息，包括：
     - 完整错误输出；
     - SMTP 主机/端口；
     - TLS/SSL 开关配置；
     - 鉴权模式；
     - 网络连通性检查结果；
     - 邮件服务商策略限制（如 app password/OAuth、发件域限制）；
     - 本次发送的 FROM/TO 信息。
   - 回退文件（`.email_fallback/email_*.txt`）中同步写入上述排障信息，便于离线定位。
2. 更新 `tests/test_send_email.py`：
   - 增加对 `build_smtp_info_request(...)` 的单元测试，校验关键排障字段存在。
   - 扩展 SMTP 失败回退文件测试，验证 fallback 内容包含排障信息清单。

测试执行结果:
- 命令: ./scripts/run_tests.sh
- 结果: 12 passed, 0 failed

邮件发送说明:
- 使用工具: python3 tools/send_email.py
- 收件人: ihuangyiran@icloud.com
