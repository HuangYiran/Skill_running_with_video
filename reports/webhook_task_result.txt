任务来源: Feishu Webhook
任务内容: I did not receive any email during last task. Check the send email method and make sure the message will be sent to ihuangyiran@icloud.com

代码修改摘要:
1. 更新 `tools/send_email.py`：
   - 增加 `SMTP_USE_SSL` 支持，兼容 SMTPS(465) 场景。
   - 对 STARTTLS 增加 `ehlo -> starttls -> ehlo` 标准流程。
   - 新增 `--allow-fallback-success` 参数。
   - 将默认失败语义改为“SMTP 失败时退出码 1”，避免任务系统误判为“已发送成功”。
2. 扩展 `tests/test_send_email.py`：
   - 新增发送成功路径测试（验证调用 `send_message`）。
   - 新增 SMTP 失败回退文件测试（验证会写入 `.email_fallback`）。
   - 新增 `main()` 退出码行为测试（默认失败退出 1；显式允许 fallback 时退出 0）。

测试执行结果:
- 命令: ./scripts/run_tests.sh
- 结果: 11 passed, 0 failed

邮件发送说明:
- 使用工具: python3 tools/send_email.py
- 收件人: ihuangyiran@icloud.com
